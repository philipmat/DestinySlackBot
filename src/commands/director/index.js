import dailyChapter from './dailyChapter';
import weeklyHeroic from './weeklyHeroic';

export default [
    dailyChapter,
    weeklyHeroic
];

let commands = {
    dailyChapter,
    weeklyHeroic
};

export {commands};